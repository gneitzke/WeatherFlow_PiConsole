# Dev/test deps only. The suite stubs Kivy (see tests/conftest.py) so none of
# these pull in Kivy/SDL/GL — every one is a pure wheel and installs in seconds.
pytest==8.3.4
pytz==2025.2
ephem==4.2
packaging==25.0
requests==2.32.3
tzlocal==5.3.1    # lib/config.py imports get_localzone; matches wfpiconsole.sh's runtime pin
